hooks:
  - name: rustfmt
    on_event: pre-commit
    on_file_regex: "*.rs"
    action: "rustfmt {files}"
    setup_script: rustfmt_setup.sh
  - name: cargofmt
    on_event: pre-commit
    on_file_regex: "*.rs"
    action: "cargo fmt"
    setup_script: rustfmt_setup.sh